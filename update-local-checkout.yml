---
- name: Update Kolide local checkout
  hosts: 127.0.0.1
  connection: local
  gather_facts: no
  become: yes
  become_method: sudo

  tasks:

  - name: git pull updates
    git:
      repo: https://github.com/cyberguerrilla/kolide-fleet.git
      dest: /opt/playbooks
      update: yes
